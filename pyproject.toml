[project]
name = "mcp-server-agenda"
version = "0.2.0"
description = "MCP server for Agenda app with full x-callback-url support"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [ "mcp>=1.1.1",]
[[project.authors]]
name = "alexgoller"
email = "83632450+alexgoller@users.noreply.github.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
mcp-server-agenda = "mcp_server_agenda:main"
